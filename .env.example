# Supabase — safe to expose in the frontend
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-only — set these in the Vercel dashboard, never in frontend files
GEMINI_API_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
